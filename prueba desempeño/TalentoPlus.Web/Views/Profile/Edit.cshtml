@model TalentoPlus.Web.Models.Employee

@{
    ViewData["Title"] = "Editar Mi Perfil";
}

<div class="max-w-2xl mx-auto">
    <!-- Header -->
    <div class="mb-8">
        <h1 class="text-4xl font-bold text-gray-900">Editar Mi Perfil</h1>
        <p class="text-gray-600 mt-1">Actualiza tu información personal</p>
    </div>

    <div class="card">
        <form asp-action="Edit" asp-route-id="@Model.Id" method="post" class="space-y-6">
            <input type="hidden" asp-for="Id" />

            <!-- Success/Error Messages -->
            @if (ViewBag.Message != null)
            {
                <div class="bg-green-50 border-l-4 border-green-500 p-4">
                    <p class="text-green-900">✓ @ViewBag.Message</p>
                </div>
            }

            <div asp-validation-summary="All" class="bg-red-50 border-l-4 border-red-500 text-red-700 p-4" />

            <!-- Read-Only Fields -->
            <div class="grid grid-cols-2 gap-6 bg-gray-50 p-6 rounded-lg">
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Nombre</label>
                    <input type="text" value="@Model.FirstName" disabled class="w-full px-4 py-2 bg-gray-200 text-gray-600 rounded-lg" />
                </div>
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Apellido</label>
                    <input type="text" value="@Model.LastName" disabled class="w-full px-4 py-2 bg-gray-200 text-gray-600 rounded-lg" />
                </div>
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Documento</label>
                    <input type="text" value="@Model.Document" disabled class="w-full px-4 py-2 bg-gray-200 text-gray-600 rounded-lg" />
                </div>
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Correo</label>
                    <input type="email" value="@Model.Email" disabled class="w-full px-4 py-2 bg-gray-200 text-gray-600 rounded-lg" />
                </div>
            </div>

            <p class="text-xs text-gray-600 italic">
                ℹ️ Los campos sombreados no pueden editarse. Contacta a Recursos Humanos para cambios en nombre, documento, correo o datos profesionales.
            </p>

            <!-- Editable Fields -->
            <div class="space-y-6 border-t pt-6">
                <div>
                    <label asp-for="Phone" class="block text-sm font-medium text-gray-700 mb-2">Teléfono</label>
                    <input asp-for="Phone" type="tel" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="+57 (1) 1234567" />
                    <span asp-validation-for="Phone" class="text-red-500 text-sm"></span>
                </div>

                <div>
                    <label asp-for="Address" class="block text-sm font-medium text-gray-700 mb-2">Dirección</label>
                    <input asp-for="Address" type="text" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Calle, número, ciudad" />
                    <span asp-validation-for="Address" class="text-red-500 text-sm"></span>
                </div>

                <div class="grid grid-cols-2 gap-6">
                    <div>
                        <label asp-for="BirthDate" class="block text-sm font-medium text-gray-700 mb-2">Fecha de Nacimiento</label>
                        <input asp-for="BirthDate" type="date" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" />
                        <span asp-validation-for="BirthDate" class="text-red-500 text-sm"></span>
                    </div>
                    <div>
                        <label asp-for="EducationLevel" class="block text-sm font-medium text-gray-700 mb-2">Nivel Educativo</label>
                        <select asp-for="EducationLevel" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
                            <option value="">Seleccionar...</option>
                            <option value="Bachiller" selected="@(Model.EducationLevel == "Bachiller")">Bachiller</option>
                            <option value="Técnico" selected="@(Model.EducationLevel == "Técnico")">Técnico</option>
                            <option value="Tecnólogo" selected="@(Model.EducationLevel == "Tecnólogo")">Tecnólogo</option>
                            <option value="Profesional" selected="@(Model.EducationLevel == "Profesional")">Profesional</option>
                            <option value="Especialista" selected="@(Model.EducationLevel == "Especialista")">Especialista</option>
                            <option value="Maestría" selected="@(Model.EducationLevel == "Maestría")">Maestría</option>
                            <option value="Doctorado" selected="@(Model.EducationLevel == "Doctorado")">Doctorado</option>
                        </select>
                        <span asp-validation-for="EducationLevel" class="text-red-500 text-sm"></span>
                    </div>
                </div>

                <div>
                    <label asp-for="ProfessionalProfile" class="block text-sm font-medium text-gray-700 mb-2">Perfil Profesional</label>
                    <textarea asp-for="ProfessionalProfile" rows="6" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Describe tu experiencia, habilidades y objetivos profesionales..."></textarea>
                    <p class="text-xs text-gray-500 mt-2">Máximo 2000 caracteres</p>
                    <span asp-validation-for="ProfessionalProfile" class="text-red-500 text-sm"></span>
                </div>
            </div>

            <!-- Action Buttons -->
            <div class="flex gap-3 pt-6 border-t">
                <button type="submit" class="btn-primary flex-1">
                    💾 Guardar Cambios
                </button>
                <a asp-action="Index" class="btn-secondary flex-1">
                    ← Cancelar
                </a>
            </div>
        </form>
    </div>

    <!-- Info Box -->
    <div class="mt-6 card bg-blue-50 border-l-4 border-blue-500">
        <div class="flex gap-4">
            <div class="text-2xl">💡</div>
            <div>
                <h4 class="font-semibold text-gray-900">Ayuda</h4>
                <ul class="text-gray-700 text-sm mt-2 space-y-1 list-disc list-inside">
                    <li>Completa toda la información que puedas para mejorar tu perfil</li>
                    <li>Tu perfil profesional es importante para las oportunidades internas</li>
                    <li>Los cambios se guardan inmediatamente</li>
                    <li>Para cambios en datos profesionales contacta a Recursos Humanos</li>
                </ul>
            </div>
        </div>
    </div>
</div>
